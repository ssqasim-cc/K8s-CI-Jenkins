package io.digital.supercharger.mapper;

final class MapperConstant {

    /**
     * This prevents the default parameter-less constructor from being used elsewhere in your code.
     */
    private MapperConstant() {
        //not called
    }

    static final String SPRING_COMPONENT = "spring";
}
